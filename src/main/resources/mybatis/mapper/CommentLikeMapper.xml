<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.homss.server.mapper.CommentLikeMapper">

    <delete id="deleteAll">
        DELETE FROM comment_like
    </delete>

    <select id="findAll" resultType="comment_like">
        SELECT cl.*
        FROM comment_like AS cl
    </select>

    <select id="findLikeStatus" resultType="boolean">
        SELECT COUNT(*)>0
        FROM comment_like AS cl
        WHERE cl.comment_id = #{commentId} AND cl.member_id = #{memberId}
    </select>

    <insert id="save">
        INSERT INTO comment_like (comment_id, member_id)
        VALUES (#{commentId}, #{memberId})
    </insert>

    <delete id="deleteLike">
        DELETE FROM comment_like AS cl
        WHERE cl.comment_id = #{commentId} AND cl.member_id = #{memberId}
    </delete>

</mapper>