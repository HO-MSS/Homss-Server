<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.homss.server.mapper.BoardMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="boardId">
        INSERT INTO board (member_id, type, title, content)
        VALUES (#{memberId}, #{type}, #{title}, #{content})
    </insert>

    <select id="findAll" resultType="board">
        SELECT board_id, member_id, content, board_type, title, board_status, created_at, updated_at
        FROM board
    </select>

</mapper>